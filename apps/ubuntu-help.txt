Ubuntu Help -- updated: 2026-06-02 08:09



(1) Migration:



(1.1) aptitude, install:

sudo apt-get install aptitude



(1.2) vim, install:

sudo aptitude install vim ;



(1.3) chromium-browser, install / run:

sudo aptitude install chromium-browser ;

chromium-browser ;



(1.4) dbeaver, install / run:

download tar.gz ;

copy to /opt/ ; (...)/dbeaver



(1.5) rabbitmq-server, install as service:

sudo aptitude search rabbitmq ;

sudo aptitude install rabbitmq-server ;



(1.6) rabbitmq-server, check service:

systemctl list-units ;

systemctl status rabbitmq-server ;

systemctl show rabbitmq-server ;



(1.7) rabbitmq-plugins, enable web-gui:

rabbitmq-plugins enable rabbitmq_management ;



(1.8) apparmor.service, stop:

systemctl status apparmor ;

systemctl stop apparmor ;

systemctl disable apparmor ;



(fail) mysql, install / run:

download tar.xz (64 bit, linux generic), failed: problems with apparmor.service



(1.9) mysql, install / run:

sudo aptitude install mysql-server-8.0 ;
